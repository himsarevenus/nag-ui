part of 'index.dart';

class NAGSizedBox extends SizedBox {
  const NAGSizedBox.textSpacing([double height = 4, Key? key])
      : super(key: key);
  const NAGSizedBox.heightXxl([double height = NAGSpacing.xxl, Key? key])
      : super(key: key);
  const NAGSizedBox.heightXl([double height = NAGSpacing.xl, Key? key])
      : super(key: key);
  const NAGSizedBox.height([double height = NAGSpacing.main, Key? key])
      : super(key: key);
  const NAGSizedBox.heightMedium([double height = NAGSpacing.medium, Key? key])
      : super(key: key);
  const NAGSizedBox.heightSmall([double height = NAGSpacing.small, Key? key])
      : super(key: key);
  const NAGSizedBox.heightxs([double height = NAGSpacing.xs, Key? key])
      : super(key: key);
  const NAGSizedBox.widthXl([double width = NAGSpacing.xl, Key? key])
      : super(key: key);
  const NAGSizedBox.width([double width = NAGSpacing.main, Key? key])
      : super(key: key);
  const NAGSizedBox.widthMedium([double width = NAGSpacing.medium, Key? key])
      : super(key: key);
  const NAGSizedBox.widthSmall([double width = NAGSpacing.small, Key? key])
      : super(key: key);
  const NAGSizedBox.widthxs([double width = NAGSpacing.xs, Key? key])
      : super(key: key);
}
